% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/representation.R
\name{build_representation}
\alias{build_representation}
\title{Build one representation for a set of targets}
\usage{
build_representation(rep, series, targets, spec)
}
\arguments{
\item{rep}{A representation from \code{\link[=native]{native()}}, \code{\link[=grain]{grain()}}, \code{\link[=multigrain]{multigrain()}} or \code{\link[=lookback]{lookback()}}.}

\item{series}{The long table of readings, or \code{NULL} where the targets carry the whole predictor
block.}

\item{targets}{The table of prediction targets.}

\item{spec}{The resolved settings \code{\link[=timesift]{timesift()}} carries: the identifier, time, value, anchor and
static columns it settled on.}
}
\value{
A \code{timesift_matrix}.
}
\description{
Turns a representation and the two tables into the \verb{[target, bin, channel]} array a learner is
fitted on. It is the one place the fitting layer builds an array, so \code{\link[=timesift]{timesift()}} and
\code{\link[=predict.timesift]{predict.timesift()}} reach a record the same way and a candidate refitted on new targets is
built from the settings its own arm was.
}
\details{
The rows are the targets, in the order the fitting layer keeps them: sorted by identifier where
one target row belongs to each unit, and in the targets' own order where \code{target_time} anchors
them. Columns named in \code{static} are appended as channels holding one value per target, constant
across the bins.
}
