% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_metric}
\alias{register_metric}
\alias{metrics}
\title{Register a metric}
\usage{
register_metric(name, fn, overwrite = FALSE)

metrics()
}
\arguments{
\item{name}{Name the metric is asked for by.}

\item{fn}{A function of \verb{(y, p)}, the observed values and the predictions for one cell, both
vectors of the same length, returning one number or \code{NA} where the cell defines none.}

\item{overwrite}{Replace an existing registration.}
}
\value{
The registered function, invisibly.
}
\description{
A metric scores one held-out cell: the observed response of the units in a fold and a model's
predictions for them. Registering one makes it available to \code{\link[=grain_ladder]{grain_ladder()}} by name, with no
change to the fitting code.
}
\examples{
register_metric("hit_rate", function(y, p) mean((p >= 0.5) == (y == 1)), overwrite = TRUE)
"hit_rate" \%in\% metrics()

}
