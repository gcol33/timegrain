% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_matrix.R
\name{feature_matrix}
\alias{feature_matrix}
\title{Bring an already-reduced feature table into a ladder}
\usage{
feature_matrix(m, label = "features")
}
\arguments{
\item{m}{A matrix or data frame of units by features, with unit identifiers in the row names or
in a leading character or factor column.}

\item{label}{The name the arm is reported under.}
}
\value{
A \code{timesift_matrix} of shape \verb{[unit, feature, 1]}.
}
\description{
A representation the package did not build, such as a published set of hand-aggregated climate
summaries, enters here. It becomes a one-channel \verb{[unit, feature, 1]} array, which is what a
learner reads, so a feature table and a temporal grain can be arms of the same \code{\link[=grain_ladder]{grain_ladder()}}
and be scored on the same cells by the same rule.
}
\details{
It carries no time axis, because it has none: the reduction already happened, elsewhere, and
what reaches the model is a list of numbers per unit. That is the whole point of comparing
against it.
}
\examples{
m <- matrix(rnorm(30), nrow = 10,
            dimnames = list(sprintf("p\%02d", 1:10), paste0("bio", 1:3)))
feature_matrix(m)

}
