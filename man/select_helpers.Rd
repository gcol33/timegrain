% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{select_helpers}
\alias{select_helpers}
\title{Choose columns by name, by prefix or by type}
\description{
\code{y}, \code{x} and \code{static} in \code{\link[=timesift]{timesift()}} are tidyselect expressions, so a response spread over a
hundred columns is named once, as \code{y = starts_with("sp_")}, and the value columns of a series
carrying several sensors are named the same way. These are tidyselect's own helpers,
re-exported so that attaching timesift is enough to reach them and a session attaching both
packages still has one implementation of each.
}
\keyword{internal}
