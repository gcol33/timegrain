% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{roc_auc}
\alias{roc_auc}
\title{The area under the ROC curve}
\usage{
roc_auc(y, p)
}
\arguments{
\item{y}{Observed presence-absence, \code{0}/\code{1} or logical.}

\item{p}{Predicted scores for the same units, in the same order. Higher means presence.}
}
\value{
One number, or \code{NA} where the cell defines none.
}
\description{
The whole curve rather than its best point. TSS is a maximum over cuts, so a small change in a
prediction often moves it not at all and then moves it a long way; the area responds to every
reordering, which is what makes it the steadier reading when many rescorings are compared, as in
\code{\link[=bin_occlusion]{bin_occlusion()}}. Tied predictions take the average rank.
}
\examples{
roc_auc(c(0, 0, 1, 1), c(0.1, 0.2, 0.8, 0.9))

}
