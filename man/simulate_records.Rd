% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_records}
\alias{simulate_records}
\title{Simulate sensor records whose response acts at a known temporal grain}
\usage{
simulate_records(
  n = 300L,
  mechanism = c("none", "event", "season", "lag"),
  variables = 10L,
  prevalence = 0.1,
  auc = 0.75,
  from = "2021-09-01",
  days = 365L,
  step_hours = 3,
  seasonal = 8,
  offset_sd = 1,
  anomaly_sd = 1,
  anomaly_days = 2,
  offset_effect = 0,
  sensor_sd = 0.3,
  year_start = "09-01",
  seed = 1L,
  draw = 1L
)
}
\arguments{
\item{n}{Number of units to draw.}

\item{mechanism}{Which generating mechanism, see The mechanisms.}

\item{variables}{Number of response variables. Each gets its own weights within the mechanism.}

\item{prevalence}{Marginal probability of presence, shared by every variable.}

\item{auc}{Population area under the ROC curve of the driver against the response.}

\item{from}{First reading instant, \code{"YYYY-MM-DD"}, read as UTC.}

\item{days}{Length of the record in days.}

\item{step_hours}{Sampling step in hours. Must divide 24.}

\item{seasonal}{Amplitude of the seasonal cycle shared by every unit, in reading units.}

\item{offset_sd}{Standard deviation of the unit-level thermal offset.}

\item{anomaly_sd}{Marginal standard deviation of each unit's AR(1) anomaly.}

\item{anomaly_days}{Correlation time of that anomaly, in days.}

\item{offset_effect}{Weight the unit-level offset enters the driver with. At the default \code{0}
the driver reads the unit's \emph{anomaly} alone, so a window coarse enough to average the anomaly
away loses the signal. At \code{1} the offset carries the response as well, and since every window
however coarse reports the offset, every grain is then equally good: that is the
grain-invariant control, not a temporal mechanism.}

\item{sensor_sd}{Standard deviation of the measurement noise added to the latent record. The
response is generated from the latent record; the readings returned carry this noise.}

\item{year_start}{\code{"MM-DD"} boundary of the hydrological year, passed to \code{\link[=window_matrix]{window_matrix()}} when
the mechanism's bins are located. Use the same value when representing the readings.}

\item{seed}{Seed of the design: the weights and the link coefficients. Two calls with the same
\code{seed} and different \code{draw} share a design and draw independent units, which is what lets a
held-out deployment sample be drawn from the same population as a training sample.}

\item{draw}{Seed of the unit draw. Also names the units, so two draws never collide.}
}
\value{
A \code{timegrain_simulation}: a list with \code{readings}, the long table \code{\link[=window_matrix]{window_matrix()}} takes;
\code{y}, the \verb{[unit, variable]} 0/1 response; \code{driver}, the standardised driver \code{z} behind it;
\code{grain}, the true grain or \code{NA}; \code{weights}, the \verb{[reading, variable]} weights defining the
driver; \code{link}, the solved \code{b0} and \code{b1}; and \code{design}, the settings the draw is reproducible
from.
}
\description{
Draws units carrying a year of sensor readings and a multi-variable presence-absence response
whose dependence on the record is a fixed linear functional of the record at one named grain.
The grain is therefore known before anything is fitted, which is what makes the output usable
for asking whether \code{\link[=select_grain]{select_grain()}} finds it.
}
\section{What the true grain is}{

The response is driven by \verb{g_ij = sum_t w_j(t) * a_i(t)}, a weighted mean of unit \code{i}'s latent
\emph{anomaly}: the record with the seasonal cycle every unit shares and the unit's own constant
offset taken out, since neither of those is temporally located and a window of any width reports
both. The weights \code{w_j} are constant within the bins of one window and zero outside
a short stretch of them, so \code{g} is exactly a linear combination of that window's bin means. The
true grain of a mechanism is the \strong{coarsest window of \code{\link[=window_matrix]{window_matrix()}} at which \code{g} is still an
exact linear functional of the representation}: at that window and at every window whose bins
nest inside it, no information about \code{g} has been averaged away, and at any coarser window some
has. Finer windows keep the information but spread it over more coefficients, so they lose to
the true grain by variance rather than by bias, which is the tension the selection has to
resolve.
}

\section{The mechanisms}{

\describe{
\item{\code{"none"}}{No temporal signal. The driver is a standard normal drawn independently of the
record, so nothing in the readings carries information about the response and no grain is
correct. \code{grain} is \code{NA}.}
\item{\code{"event"}}{An isolated event. The weights are uniform over three consecutive \strong{day}
bins at a fixed calendar position, one position per variable. True grain \code{"day"}: a week bin
mixes the three days with four others and cannot be unmixed.}
\item{\code{"season"}}{A smooth seasonal response. The weights are uniform over one whole
\strong{season} bin, one season per variable, cycled. True grain \code{"season"}: month and day bins
nest inside a season so they are exact too, a year bin mixes all four seasons.}
\item{\code{"lag"}}{A lagged, cumulative response. The weights decay geometrically over four
consecutive \strong{week} bins from a fixed anchor, one anchor per variable. True grain \code{"week"}:
day bins nest inside weeks so they are exact too, month bins straddle week boundaries.}
}
}

\section{How the skill is set rather than emergent}{

The driver is standardised to a standard normal by its population mean and standard deviation,
both computed in closed form from the generating parameters rather than from the drawn units, so
every draw and every chunk of a draw is on the same scale. The response is
\verb{y_ij ~ Bernoulli(plogis(b0 + b1 z_ij))} with \code{b0} and \code{b1} solved by numerical integration so
that the marginal prevalence is \code{prevalence} and the population area under the ROC curve of \code{z}
is \code{auc}. \code{auc} is therefore a ceiling no fitted model reaches: the response is generated from
the latent record and the readings carry \code{sensor_sd} of measurement noise on top of it, and the
weights have to be estimated.
}

\examples{
sim <- simulate_records(n = 40L, mechanism = "event", variables = 2L, days = 60L)
sim
sim$grain
x <- window_matrix(sim$readings, unit, time, reading, window = c("day", "month"))
dim(x$day)

}
\seealso{
\code{\link[=select_grain]{select_grain()}}, which this exists to test, and \code{\link[=window_matrix]{window_matrix()}}, whose calendar the
weights are defined on.
}
