% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{timegrain_set}
\alias{timegrain_set}
\title{A ladder of representations, one per window}
\usage{
timegrain_set(x)
}
\arguments{
\item{x}{A named list of \code{\link[=window_matrix]{window_matrix()}} results, or a single one.}
}
\value{
A \code{timegrain_set}: the list, with the names as window labels.
}
\description{
Naming several windows in \code{\link[=window_matrix]{window_matrix()}} returns one of these: a named list of
representations of the same units, differing only in how coarsely the record was read. It is
what \code{\link[=window_ladder]{window_ladder()}} fits across.
}
\examples{
t <- seq(as.POSIXct("2021-09-01", tz = "UTC"), by = "hour", length.out = 24 * 60)
d <- data.frame(plot = rep(c("a", "b"), each = length(t)), t = rep(t, 2),
                temp = rnorm(2 * length(t)))
s <- window_matrix(d, plot, t, temp, window = c("day", "week", "month"))
s
names(s)

}
