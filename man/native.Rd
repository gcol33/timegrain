% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/representation.R
\name{native}
\alias{native}
\alias{grain}
\alias{multigrain}
\alias{lookback}
\title{How a series becomes an array a learner reads}
\usage{
native(stats = "mean", year_start = "09-01")

grain(grain, stats = "mean", year_start = "09-01")

multigrain(grains = NULL, stats = "mean", year_start = "09-01")

lookback(span, lag = "0 days", bins = 1L, stats = "mean")
}
\arguments{
\item{stats}{Statistics computed per bin, one channel each, in the order given. See
\code{\link[=grain_matrix]{grain_matrix()}} for the seven and for what separates an extreme reading from an extreme day.}

\item{year_start}{\code{"MM-DD"} boundary of the hydrological year, used by \code{"season"} and \code{"year"}.}

\item{grain}{One of \code{"native"}, \code{"halfday"}, \code{"day"}, \code{"week"}, \code{"month"}, \code{"season"},
\code{"year"}, or a function of the reading instants returning each reading's bin start. See
\code{\link[=grain_matrix]{grain_matrix()}}.}

\item{grains}{Grains bound side by side into one block, or \code{NULL} for the automatic set
\code{\link[=grains]{grains()}} describes.}

\item{span}{The lookback's length, as a duration such as \code{"30 days"} or a number of seconds.
See \code{\link[=lookback_matrix]{lookback_matrix()}} for how a duration is read.}

\item{lag}{The gap between a target's instant and the end of its lookback.}

\item{bins}{Sub-bins the lookback is cut into, oldest first. One gives a block of features,
several give a sequence.}
}
\value{
A \code{timesift_representation}.
}
\description{
A representation is the reduction the package exists to make explicit: the record unreduced,
the record at a calendar grain, several grains bound into one block of features, or a lookback
of fixed length ending at each target's own instant. It carries the settings and nothing else,
so the same object describes a representation before any record has been seen, names the arm it
produced in a fitted object, and rebuilds itself for new targets in \code{\link[=predict.timesift]{predict.timesift()}}.
}
\details{
Every representation carries \code{label}, the name it is reported under; \code{kind}, one of \code{"grain"},
\code{"multigrain"} and \code{"lookback"}; the settings its kind uses; and \code{sequence}, which says whether
its bins are ordered in time and so mean something to a convolution. \code{native()}, \code{grain()} and a
\code{lookback()} of more than one bin are sequences; \code{multigrain()} and a one-bin \code{lookback()} are
blocks of features.

\code{multigrain()} flattens each of its grains to one row per target and puts them side by side, so
a column of the block names the grain, the statistic and the bin it came from. It is the
tabular representation a penalised regression or a random forest reads.
}
\examples{
native()
grain("week", stats = c("cold_day", "mean", "warm_day"))
multigrain(c("month", "season"))
lookback("30 days", bins = 3L)

}
